<!DOCTYPE html>
<html>
    <body>
        <h1>게시글 목록 페이지</h1>

        <a href="/articles/regist?id=test&stock=3">신규 글 작성</a>
        <table>
            <thead>
                <tr>
                    <th>글번호</th>
                    <th>제목</th>
                    <th>내용</th>
                    <th>IP</th>
                    <th>조회수</th>
                    <th>글쓴이</th>
                    <th>작성일</th>
                </tr>
            </thead>

            <tbody>

                <% for(var i =0;i<articles.length;i++){ %>
                    <tr>
                        <!-- url 파라미터 방식으로 값 전달 -->
                        <td><%=articles[i].idx%></td>
                        <td>
                            <a href="/articles/<%=articles[i].idx%>">
                                <%=articles[i].title%>
                            </a>
                        </td>
                        <td>
                            <!-- URL QueryString 방식으로 값 전달  -->
                            <a href="/articles/article?idx=<%=articles[i].idx%>">
                                <%=articles[i].contents%>
                            </a>
                        </td>
                        <td><%=articles[i].ip%></td>
                        <td><%=articles[i].viewcnt%></td>
                        <td><%=articles[i].register%></td>
                        <td><%=articles[i].registDate%></td>
                    </tr>
                <% } %>

            </tbody>
        </table>
    </body>
</html>